export interface Archive {
    bundleId: string
    date: string
    name: string
    title: string
    comments: string
    urls: string[]
}

export interface Bundle {
    bundleId: string
    title: string
    date: string
}